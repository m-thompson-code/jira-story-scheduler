import { jiraStoryScheduler } from './jira-story-scheduler';

describe('jiraStoryScheduler', () => {
  it('should work', () => {
    expect(jiraStoryScheduler()).toEqual('jira-story-scheduler');
  });
});
